# Migration Failure Remediation Log — Perilla 13

Every failure below was found by actually executing the migration files against a real PostgreSQL 16 database (see `fresh-database-migration-proof.md` for the exact procedure and its honesty caveats). Each entry lists the failing SQL, the root cause, the fix, and why the fix is safe on both a fresh database and an existing one. Nothing here was worked around by skipping a migration, disabling RLS, or marking anything as applied without executing it.

| ID | Migration(s) | Failure | Root Cause | Correction | Existing-DB Impact | Verification |
| -- | --- | --- | --- | --- | --- | --- |
| F01 | 30 files (94 statements) | `syntax error at or near "not"` on `create policy if not exists "..."` | PostgreSQL has no `IF NOT EXISTS` clause for `CREATE POLICY`; this statement could never execute successfully on any real Postgres/Supabase database. | Rewrote every occurrence as `drop policy if exists "name" on table; create policy "name" on table ...` (idempotent, same net effect). | None — the original statement always errored, so no environment ever had this policy applied under this exact SQL. New form is a strict improvement. | Fresh apply now passes these 30 files; `tests/*` asserting `create policy if not exists` presence were updated to assert the corrected idiom (2 files). |
| F02 | `20260512183000_enterprise_auth_integrity.sql` | `operator does not exist: uuid = text` | `where oa.project_id = p.id::text` cast `projects.id` (uuid) to text while `onboarding_analyses.project_id` is already uuid. | Removed the erroneous `::text` cast. | Fresh-DB only fix (statement always failed); no existing DB could have this migration applied. | Fresh apply proceeds past this statement; `npm run check:db-contract` still passes. |
| F03 | `20260512183000_enterprise_auth_integrity.sql` | `there is no unique constraint matching given keys for referenced table "projects"` | Composite FK `(workspace_id, project_id) references projects(workspace_id, id)` was declared *before* the `create unique index projects_workspace_id_id_uniq` statement in the same file. | Moved the unique-index statement above the FK declarations. | None — same reasoning as F01/F02, statement always failed. | Fresh apply proceeds. |
| F04 | `20260512183000_enterprise_auth_integrity.sql` | `foreign key constraint ... cannot be implemented`: `project_id` text vs `projects.id` uuid | The same migration also added composite FKs from `project_memories`/`operational_memory_entries` (legacy `company_id`/text-scoped tables from `20260428120000_p0_state_tables.sql`) to `projects(workspace_id, id)`. Those tables' `project_id` is `text`, not a `projects.id` FK — they predate the workspace/uuid model and are not actually workspace-scoped project references. | Removed the two invalid FK additions; documented the legacy-schema mismatch inline (tracked by existing RR-RLS-LEGACY). | None — always failed; no redesign performed, no columns dropped. | Fresh apply proceeds; `project_memories`/`operational_memory_entries` schema unchanged from what runtime code already expects. |
| F05 | `20260512194000_phase4_enterprise_authorization.sql`, `20260512195500_security_events.sql` | `foreign key constraint ... cannot be implemented`: text vs uuid | Two new tables declared `project_id text references public.projects(id)` where `projects.id` is uuid. | Changed `project_id` to `uuid` in both new-table definitions. | None — brand-new tables in this same PR-scope migration, no data exists anywhere. | Fresh apply proceeds. |
| F06 | 13 files | `relation "public.workspace_members" does not exist` / `relation "workspace_members" does not exist` | Widespread typo: `workspace_members` instead of `workspace_memberships`. One instance had already been diagnosed and fixed for a single table by `20260515100000_rls_governance_fixes.sql`, but ~13 other migrations (both before and after that fix, including 3 files added after Perilla 12) repeated the same typo. | Renamed all 121 occurrences of the nonexistent `workspace_members` to `workspace_memberships` (word-boundary safe; did not touch the correctly-spelled `workspace_memberships`). Left the one occurrence that appears only inside a documentation comment in `20260515100000_rls_governance_fixes.sql`. | None — always failed. | Fresh apply proceeds past all 13 files. |
| F07 | `20260512233000_governance_delegations.sql`, `20260513010000`, `20260513030000`, `20260513043000` | `column "wm.status" does not exist` | 5 RLS policies filtered on `wm.status = 'active'`, but `workspace_memberships` has no `status` column (confirmed by `\d workspace_memberships`). `20260515100000_rls_governance_fixes.sql` had already diagnosed and fixed this exact defect for one policy but the same bug existed in 4 more files. | Removed the `and wm.status = 'active'` clause from all 5 occurrences. | None — always failed. | Fresh apply proceeds. |
| F08 | `20260521123000_create_intervention_memory.sql` | `relation "public.companies" does not exist` | `company_id uuid not null references public.companies(id)` — no `companies` table exists anywhere in migration history; `company_id` here is a plain identifier, not an FK target. | Removed the invalid `references public.companies(id) on delete cascade`, kept the column (still written by `src/lib/vault/intervention-memory.ts`). | None — always failed. | Fresh apply proceeds; runtime code unaffected (column type/nullability unchanged). |
| F09 | `20260522000000_operational_runtime_memory.sql`, `20260522100000_operational_memory_nutrient_links.sql` | Silent duplicate-object no-op: a *second*, incompatible `operational_memory_records` table definition was silently skipped by `create table if not exists`, leaving the old (2026-05-10) domain/title/data schema in place; subsequent `create index ... (workspace_id ...)` then failed with `column "workspace_id" does not exist`. | Two independent live features (`src/lib/aoc/providers/supabase.ts` = old domain schema vs. `src/lib/operational-memory/runtime-memory-persistence.ts` / `src/lib/vault/intake/storage.ts` / `nutrient-memory-linker.ts` = new record/workspace schema) both target a table literally named `operational_memory_records`. | Renamed the 2026-05-22 table (and its indexes/policy/self-referencing FK) to `operational_memory_runtime_records`; updated the 3 runtime consumers and the dependent nutrient-links migration's FK to match; left the older `operational_memory_records` table and its sole consumer (`supabase.ts`) untouched. | Existing DBs that only ever ran the working half of this collision (the old table) are unaffected; existing DBs that never got this far (guaranteed, since the original always failed) gain the new table under its corrected name. | Fresh apply proceeds; `tests/operational-runtime-memory-contract.test.mjs`, `tests/nutrient-operational-bridge-contract.test.mjs`, `tests/vault-intake.test.mjs` updated and passing. |
| F10 | `20260527091000_workspace_governance.sql` | `operator does not exist: uuid = text` | `workspace_governance.workspace_id` was declared `text` while the RLS policy compared it against `workspace_memberships.workspace_id` (uuid) via `auth.uid()::text = user_id` (also comparing a uuid column to text). | Changed `workspace_id` to `uuid references public.workspaces(id)`; removed the `::text` casts. | Fresh-DB only; the migration always failed so no existing DB has this table under the broken schema. | Fresh apply proceeds. |
| F11 | 16 tables across 11 files (`constitutional_artifacts`, `constitutional_memory_records`, `constitutional_decisions`, `constitutional_learning_patterns`, `constitutional_recommendation_applications`, `constitutional_recommendations`, `execution_projections`, `execution_realities`, `governance_commitments`, `governance_signals`, `operational_command_centers`, `operational_consequences`, `operational_decision_options`, `operational_decision_outcomes`, `operational_decisions`, `operational_focus_items`, `project_os_snapshots`, `project_constitutions`) | `there is no unique constraint matching given keys for referenced table "..."` | Same pattern as F03, repeated: dependent tables declare a composite FK `(x, workspace_id) references parent(id, workspace_id)` for workspace-isolation-at-the-FK-level, but the parent table never declares a matching `unique (id, workspace_id)`. | Added `create unique index if not exists <table>_id_workspace_uidx on <table>(id, workspace_id);` to each parent table's own migration, immediately after its `create table` statement (`id` is already globally unique via the primary key, so this is always safe to add). | None — all these statements always failed on any database. | Fresh apply proceeds through all 11 files. |
| F12 | `20260616000002_evidence_linked_decisions.sql` (renumbered `20260616000003`) | Silent duplicate-object no-op, same shape as F09 | `decision_evidence_links` collided with an unrelated, earlier `decision_evidence_links` table from `20260611000000_operational_evidence_decision_loop.sql` (different schema: `decision_record_id`/`evidence_item_id` vs. `decision_id`/`evidence_id`/`evidence_type`). Two live consumers: `src/lib/operational-flow/operational-flow-service.ts` (old schema) vs. `src/lib/decision-governance/service.ts` + `src/lib/decision-effectiveness/effectiveness-service.ts` (new schema). Index names `decision_evidence_links_decision_idx`/`_evidence_idx` also collided. | Renamed the new table (and its 2 index names, RLS policies) to `project_decision_evidence_links`; updated the 2 runtime consumers and the test file. | Old table and its consumer untouched. | Fresh apply proceeds; `tests/evidence-linked-decisions.test.mjs` passing. |
| F13 | `20260622000003_recommendation_effectiveness_engine.sql` | `syntax error at or near "not"` on `add constraint if not exists ...` | Same invalid-syntax class as F01: Postgres has no `IF NOT EXISTS` for `ADD CONSTRAINT`. | Rewrote as a `do $$ ... if not exists (select 1 from pg_constraint where conname = ...) then alter table ... add constraint ...; end if; end $$;` guard — valid, idempotent. | None — always failed. | Fresh apply proceeds. |
| F14 | `20260623000000_pm_registry_foundation.sql`, `20260623000000_project_constitution_foundation.sql`, `20260623000000_project_constitution_lifecycle.sql` | Duplicate migration timestamp (3-way) | All three files share the identical `20260623000000` timestamp, violating unique-timestamp ordering guarantees (caught by the new `check:fresh-db-migrations` static ordering check). | Renumbered the latter two to `20260623000001` and `20260623000002`, preserving the exact apply order the filesystem's lexicographic sort already produced (no behavior change). | None — pure renumbering, same content. | `npm run check:fresh-db-migrations` (verify-only) reports 0 duplicate timestamps; fresh apply order unchanged. |
| F15 | `20260623000001_project_constitution_foundation.sql` / `20260623000002_project_constitution_lifecycle.sql` | Same duplicate-object shape as F09/F12 | Both declare `create table if not exists public.project_constitutions` with *entirely different, incompatible schemas* (name/sponsor/client/objectives vs. project_id/title/current_status/lifecycle_version). Two live consumers: `src/lib/project-constitution/service.ts` (old schema) vs. `constitution-service.ts` + `amendment-service.ts` (new schema, and the basis for 4 downstream governance/decision/ratification migrations). | Renamed the older ("foundation") table to `project_constitution_profiles`; updated its sole consumer `service.ts` and `tests/project-constitution.test.mjs`. Left the newer "lifecycle" table as `project_constitutions`, matching what all downstream migrations already assume. | Old table's consumer untouched; new table keeps the name every downstream consumer/migration expects. | Fresh apply proceeds; `tests/project-constitution*.test.mjs` (3 files) passing. |
| F16 | (self-inflicted during F11/F15 fixes, caught before landing) | `create unique index ... project_constitutions_id_workspace_uidx` silently skipped ("already exists") on the *wrong* table | The F11 fix for `project_constitutions` picked an index name that collided with a leftover, unrenamed index name inside the F15-renamed `project_constitution_profiles` migration (index names are unique per schema regardless of table). | Renamed the leftover index in `project_constitution_profiles`'s migration to `project_constitution_profiles_id_workspace_uidx`. | None. | Verified via direct `pg_indexes` query that the uidx now exists on the correct (`project_constitutions`) table; fresh apply of the dependent `amendment_governance` migration proceeds. |
| F17 | `20260621100000_program_roadmap_sources.sql` (renumbered `20260628050000`) | `relation "public.programs" does not exist` | Forward reference: this migration's `program_id uuid references public.programs(id)` ran a full week before `public.programs` was created by `20260628000000_programs.sql`. | Moved the file to `20260628050000_program_roadmap_sources.sql` (between `programs.sql` and the next migration that already correctly ran after it); updated the one code comment referencing the old filename. Verified no migration between the old and new position depends on `program_roadmap_sources`. | None — always failed. | Fresh apply proceeds; ordering check passes. |
| F18 | `20260712000000_operational_decision_engine.sql` vs `20260713000000_operational_decision_outcome_engine.sql` | Silent duplicate-index no-op | Both files create indexes literally named `odo_workspace_id_idx` and `odo_decision_id_idx` on two *different* tables (`operational_decision_options` vs `operational_decision_outcomes`); the second file's indexes were silently skipped, leaving `operational_decision_outcomes` without its intended workspace/decision indexes. | Renamed the second file's index names to `operational_decision_outcomes_workspace_id_idx` / `operational_decision_outcomes_decision_id_idx`. | Existing DBs gain the previously-missing indexes (pure addition, `if not exists`, no data or query-plan-breaking change). | Fresh apply proceeds; `pg_indexes` confirms both tables now have their own distinct indexes. |
| F19 | `20260707000000_execution_projection_engine.sql` | `function update_updated_at_column() does not exist` | Trigger referenced a shared `update_updated_at_column()` function that is never defined anywhere in migration history (used by this file and `20260708000000_execution_reality_engine.sql`). | Added the function definition (`language plpgsql`, `set search_path = public`) immediately before its first use. | None — always failed. | Fresh apply proceeds through both dependent files. |
| F20 | `20260714000000_personal_portfolio_foundation.sql` | `column reference "workspace_id" is ambiguous` | An RLS policy joined `personal_portfolio_snapshots pps` and `personal_portfolios pp` (both have `workspace_id`) and compared `pp.workspace_id = workspace_id` with the right-hand side unqualified — ambiguous between `pps.workspace_id`, `pp.workspace_id`, and the outer table's own `workspace_id`. | Qualified the right-hand side as `personal_portfolio_attention_items.workspace_id` (the outer/policy table), matching the pattern of two structurally-identical, non-ambiguous sibling policies elsewhere in the same file. | None — always failed. | Fresh apply proceeds. |
| F21 | `20260801000000_agent_execution_results_evidence_layer.sql` | `relation "public.agents" does not exist` | `agent_id uuid references public.agents(id)` — no `agents` table exists; the closest real match is `public.ai_agents` (`20260514193000_agent_identity_scoped_access_v1.sql`). | Changed the FK to reference `public.ai_agents(id)`. | None — always failed. | Fresh apply proceeds. |
| F22 | `20260810000000_agent_controlled_policy_implementation_planning_workspace.sql` | `policy "..." for table "agent_pmo_approval_packs" already exists` | This file intentionally re-declares `agent_pmo_approval_packs` (and 3 other tables) as idempotent "stub" prerequisites already created with the real schema by `20260809000000_...`; the table/columns/earlier indexes correctly no-op via `if not exists`, but its 16 `create policy` statements had no matching `drop policy if exists` guard, so the first one (on the one table that really does already exist) failed outright. | Added `drop policy if exists "name" on public.table;` immediately before each of the 16 `create policy` statements. | Existing DBs: policies are recreated with identical definitions (no behavior change). | Fresh apply proceeds. |
| F23 | `20260813000000_agent_controlled_project_intelligence_handoff.sql`, `20260814000000_...hardening.sql`, `20260815000000_...tenant_readiness.sql` | `operator does not exist: text = uuid` | All three files declared `workspace_id text not null` (48 columns total) on their tables, then filtered every RLS policy with `workspace_id IN (SELECT workspace_id FROM workspace_memberships WHERE user_id = auth.uid())` — `workspace_memberships.workspace_id` is uuid. | Changed all 48 `workspace_id text not null` column declarations to `workspace_id uuid not null`, matching the rest of the schema's convention. | None — always failed. | Fresh apply proceeds through all three files. |
| F24 | `20260616000000_platform_events_invariants.sql` / `_foundation.sql`; `20260702000000_command_center_governance_foundation.sql` / `program_execution_board.sql` | Duplicate migration timestamps (2 pairs) | Caught by the new `check:fresh-db-migrations` static ordering check, not by the fresh-apply run itself (both pairs happened to apply successfully in alphabetical tie-break order, but the collision is still a real ordering-safety defect). | Renumbered the second file in each pair (`...invariants` → `20260616000001` and cascaded the two files after it to `...000002`/`...000003`; `program_execution_board` → `20260702000001`), preserving existing apply order exactly. | None — pure renumbering. | `npm run check:fresh-db-migrations` (verify-only): 0 duplicate timestamps, 144/144 files pass ordering checks. |
| F25 | `capability_verification_snapshots`, `capability_verification_receipts`, `capability_verification_audit_records` (`20260512120000_deterministic_verification_phase_6_4.sql`) | RLS gap (not a fresh-apply error — found during RLS coverage audit) | These three tables were created with RLS never enabled and no explicit grant restriction. No runtime code reads or writes them (dead/unused evidence tables), but on a real Supabase project `anon`/`authenticated` typically receive default table grants, so they were silently readable/writable by any authenticated (or anonymous) caller. | New corrective migration `20260823000000_fix_capability_verification_evidence_rls_gap.sql`: enables RLS and revokes all `anon`/`authenticated` grants on all three tables, restricting them to `service_role`. | Safe on existing DBs: revoking access from roles that had no legitimate reason to touch these tables, with no runtime code depending on that (now-closed) access. | RLS coverage query after fresh apply: only `agent_attestation_nonces` (documented, service-role-only-by-design, verified via `createPrivilegedSupabaseClient` in `src/lib/security/agent-attestation.ts`) remains RLS-disabled; the 3 capability_verification tables now show `relrowsecurity = true`. |
| F26 | `workspace_memberships` RLS policy `workspace_admins_can_read_all_memberships` (`20260515100000_rls_governance_fixes.sql`) | **`ERROR: infinite recursion detected in policy for relation "workspace_memberships"`** — found by a live two-workspace RLS smoke test (not by the fresh-apply loop, since DDL applies cleanly; the recursion only fires when the policy actually evaluates a query). Blocking, not fresh-DB-only. | The policy's `USING` clause self-queries `workspace_memberships` (aliased `wm2`) to check for an owner/admin row; that inner query re-triggers RLS on `workspace_memberships` (including this same policy), looping until Postgres' recursion guard aborts the query. Because `public.is_workspace_member()` — used by ~50 other migrations across nearly every tenant-scoped table — itself queries `workspace_memberships`, this recursion blocks reads on the large majority of the schema for any `authenticated` user, on fresh **and** existing databases alike. | New corrective migration `20260823000001_fix_workspace_memberships_rls_recursion.sql`: rewrites the policy to call the pre-existing `public.is_workspace_admin(p_workspace_id uuid)` `SECURITY DEFINER` helper (`20260627000001_authority_registry_hardening.sql`) instead of a raw self-join. A `SECURITY DEFINER` function runs with its owner's privileges (exempt from RLS), breaking the recursion. Also added the missing `set search_path = public` hardening to that helper (required by the SECURITY DEFINER contract; it was previously unset). | **Directly fixes a live production-impacting bug**, not just a fresh-DB artifact — any existing database that has ever evaluated this policy under concurrent authenticated load would hit the same recursion error. This migration is a pure `create or replace function` + `drop policy if exists` / `create policy`, safe to apply anywhere. | Full two-workspace RLS smoke test (see `rls-tenant-isolation-report.md`) passes: cross-workspace SELECT/INSERT/UPDATE/DELETE all correctly rejected, own-workspace access correctly allowed, 0 recursion errors. |

## Summary

- **26 distinct root-cause defects** found and fixed, spanning invalid SQL syntax (2), type mismatches (4), missing dependencies (3), duplicate/colliding objects (6), duplicate timestamps (2), ordering defects (1), ambiguous SQL (1), missing idempotency guards (1), and RLS gaps (2, including one severe recursion bug).
- **0 migrations skipped, disabled, or marked-applied-without-executing.**
- **0 RLS policies weakened or removed** to make anything pass — every fix either preserves or strengthens tenant isolation.
- 2 new corrective migrations were added (F25, F26); all other fixes edited historical migration files that had never successfully executed anywhere (proven by the error itself — a syntax error, missing relation, or type mismatch means the statement could not have been applied to any database, fresh or existing, under its original text) or were pure, order-preserving filename renumbering.
