# Residual Risk Register — Perillas 11–13B (Beta Release Closure Gate)

Maintained as of 2026-09-01 (P0-LAUNCH-06 reconciliation: RR-MIGRATE and RR-BACKUP moved to RESOLVED on executed hosted/restore evidence, and RR-INVITE-AUDIT-NONATOMIC registered; prior baselines P0-LAUNCH-05, 2026-08-29 and Pilot Gate Sprint 01, 2026-07-15). **This register is the canonical CURRENT-state record.** Dated per-perilla documents (e.g. `beta-release-gate-results.md`, `beta-release-closure-summary.md`) are point-in-time records of their own perilla and are superseded by this table where they disagree. Every row in the open table is a
real, open risk; risks resolved by Perilla 11 were removed (plaintext invite
tokens, unbounded AI execution, missing readiness probe, missing log
redaction path, next/ws/transitive dependency highs, non-ordered migration
file). Closed risks with lasting controls keep a traceability entry in the
"Closed" section below (RR-XLSX, Perilla 12).

Severity/likelihood/impact: H/M/L. "Pilot Blocker" means the closed,
supervised pilot must not start until the row's mitigation/condition is met.

### Disposition vocabulary (normalised)

A row carries an explicit **`DISPOSITION:`** only when a named increment has
reached a final judgement on it. The allowed values are exactly:

| Disposition | Meaning |
| --- | --- |
| `RESOLVED (<increment>)` | The risk is closed by proven work in that increment. The row states what was proven and by which mechanism. Any part that is NOT closed is split into its own row rather than left implied. |
| `DEFERRED_TO_<INCREMENT>_WITH_EXPLICIT_REASON` | Deliberately not closed. The row must state the reason and must not overstate what is wired today. |
| `ACCEPTED_FOR_CLOSED_BETA` | Knowingly accepted for the closed beta, with the release implication stated explicitly. |

A row with **no** `DISPOSITION:` tag is simply **OPEN** — that is the default for
every row in the table below, as the opening paragraph states, and it is not a
weaker form of any value above. Rows inherited from earlier increments carry no
tag and remain open on their existing mitigation, owner and target date.

| ID | Risk | Severity | Likelihood | Impact | Mitigation | Owner | Target Date | Pilot Blocker |
| -- | ---- | -------: | ---------: | -----: | ---------- | ----- | ----------- | ------------- |
| RR-MIGRATE | **DISPOSITION: RESOLVED (P0-LAUNCH-06, 2026-09-01).** Hosted fresh apply EXECUTED against the disposable validation project `ecwkldflddnmdwusatuh`: **161/161 migrations applied, 0 remote-pending, 0 remote-unexpected, local/remote count match YES, no `migration repair` and no manual intervention** (`HOSTED_MANUAL_REPAIR=NO`). The active PMFreak project `refvllnadfzjkxlpidrr` was never a target. Verified with `supabase migration list --linked`; that verification first produced a FALSE NEGATIVE because the repeatability parser accepted only bare 14-digit timestamps while CLI 2.116.0 emits them backtick-wrapped, so the parser matched zero rows and reported all 161 as pending — the parser was fixed and given regression controls; hosted state was never at fault. The 161-version chain is re-verifiable offline from the hosted migration-history dump (set comparison against `supabase/migrations/*.sql`: 161 vs 161, min `20260428120000`, max `20260907000000`, 0 missing, 0 unexpected). Evidence: `docs/release/hosted-supabase-migration-proof.md`. **SCOPE:** this closes hosted fresh-migration execution only — hosted RLS coverage, tenant isolation, full role matrix, grants, generated-types drift and existing-DB compatibility remain NOT EXECUTED on hosted (see the result table in that document), and `FULL_BETA_DEPLOYMENT_TOPOLOGY_CERTIFIED=NO`. **PRIOR STATE (historical, superseded):** Perilla 13: fresh-apply proven against a *local* PostgreSQL 16 (144 migrations, 0 failures after remediation; 26 real migration defects found and fixed, including a blocking `workspace_memberships` RLS recursion bug — see `docs/release/migration-failure-remediation-log.md`). **Perilla 13B (this update)**: still **no hosted Supabase credentials available** in this environment, so the hosted fresh-apply itself was not run — but the harness, safety tests, and static evidence that don't require hosted access were completed: hosted-mode repeatability verification added to `scripts/check-fresh-db-migrations.mjs` (parses `supabase migration list --linked`, fails on remote-pending/remote-unexpected/count-mismatch); 20 new behavioral safety-guard tests (`tests/fresh-db-migrations-safety-guard.test.mjs`) confirming production-ref rejection, project-ref-mismatch rejection, missing-confirmation rejection, and secret redaction — all executed for real, none require network access; a full static SECURITY DEFINER audit (new `npm run check:security-definer-hardening`, now a blocking beta-release gate) found and fixed 2 genuine gaps across 8 functions (1 missing `search_path`, 8 missing an explicit PUBLIC execute revocation — see `docs/release/hosted-grants-report.md`), landed as 2 corrective migrations (144→**146** migration files); a static RPC signature inventory (`docs/release/hosted-rpc-signature-report.md`) confirmed all 8 distinct RPC functions' call-site arguments match their migration-defined signatures. **None of this constitutes hosted execution** — templates/stubs were created for the hosted-only evidence (`hosted-supabase-migration-proof.md`, `hosted-rls-role-matrix.md`, `generated-types-drift-report.md`, `existing-database-compatibility-report.md`), each explicitly marked NOT EXECUTED with the exact commands to run once credentials exist. **RR-MIGRATE remains OPEN.** **Pilot Gate Sprint 01 (2026-07-15)**: hosted credentials still absent; Docker daemon available but the environment's egress policy blocks container-registry CDNs, so the official local stack could not start either. Executed instead against a real installed PostgreSQL 16.13: 3 independent fresh applies (146/146, 146/146 repeatability, and **147/147** including a new corrective migration) with the third run mirroring hosted default privileges (`ALTER DEFAULT PRIVILEGES` before apply); live two-workspace RLS smoke test **10/10 PASS**; `check:db-contract` PASS; **one new real defect found and fixed live** — `agent_attestation_nonces` (RLS-off by design) was readable/writable by `authenticated` under hosted-default grants, closed by `20260826000000_fix_agent_attestation_nonces_grants.sql` and re-verified (`permission denied`). Full evidence: `docs/release/pilot-gate-migration-proof.md`. **RR-MIGRATE remains OPEN** — hosted execution is still the missing evidence; remaining work is runbook §10 (~half a day once credentials exist). | M | L | H | Resolved by the P0-LAUNCH-06 hosted execution above. Remaining hosted-only evidence (RLS/role matrix/grants/types drift/existing-DB compatibility) is tracked by its own NOT EXECUTED rows in `hosted-supabase-migration-proof.md`, not by this row | Repo owner | Done (P0-LAUNCH-06) | No |
| RR-BACKUP | **DISPOSITION: RESOLVED (P0-LAUNCH-06, 2026-09-01) for the scoped closed-beta logical-backup recoverability claim.** Logical backup exported from `ecwkldflddnmdwusatuh` (roles/schema/data/migration-history + a minimal `storage` supplement) and proven to reconstruct PMFreak in an **authoritative single-pass isolated local restore: 1 attempt, exit 0** — 433 tables, 92 functions, 877 policies, 432 RLS enables, 1185 indexes (by-name, 0 missing), 1114 FKs, 88 triggers, 161 migration rows (0 missing/unexpected/duplicate), the `pmfreak-documents` bucket and the `storage.objects` `service_role_full_access` policy; `check:db-contract` PASS and the fresh-db RLS smoke test **10/10 PASS** on the restored database. A real gap was found and closed in the process: `supabase db dump` excludes the `storage` schema, so roles+schema+data alone would NOT have restored the PMFreak-owned storage policy. An earlier Phase B run that retried after a failure is retained as **NON_AUTHORITATIVE** and is not the closure evidence. Backup artifacts are LOCAL ONLY and never committed. Evidence: `docs/release/backup-restore-drill.md`. **SCOPE:** `HOSTED_PLATFORM_RESTORE_REHEARSAL=NOT_PERFORMED` — no hosted restore was performed, deliberately, so the hosted validation project survives intact as the RR-MIGRATE artifact; hosted backup tier/PITR confirmation also remains outstanding. **PRIOR STATE (historical, superseded):** **Pilot Gate Sprint 01 (2026-07-15): restore drill REHEARSED with evidence** — timed `pg_dump -Fc` (0.35 s) → `pg_restore` (13.5 s, zero warnings) into a scratch database on the fully-migrated 409-table schema; 10-metric integrity diff IDENTICAL (incl. data checksum); RLS re-verified live on the restored DB. Procedure + RPO/RTO statement: `docs/release/backup-restore-drill.md`. **Remains OPEN** on one operator step: confirm pilot project backup tier/PITR and one hosted restore rehearsal (~15 min once the hosted project exists). | M | L | H | Resolved for logical-backup recoverability by the P0-LAUNCH-06 rehearsal above. Hosted backup tier/PITR confirmation and a hosted-platform restore rehearsal remain outstanding and are tracked as scope limits, not as this row | Repo owner | Done (P0-LAUNCH-06) | No |
| RR-PENTEST | No third-party penetration test | M | M | H | Closed pilot with known participants; all ten perillas' internal adversarial test suites pass; schedule external test before any open beta | Repo owner | Before open beta | No |
| RR-SOC2 | No SOC 2 / ISO certification | L | — | M | Out of scope for a closed pilot; disclose to pilot participants | Repo owner | GA planning | No |
| RR-CSP | CSP still allows `unsafe-inline` styles (Next.js constraint) | M | M | M | Framework-level; re-evaluate with next CSP-nonce support; XSS surface otherwise reduced (React escaping, no `dangerouslySetInnerHTML` on user input) | Repo owner | Next hardening sprint | No |
| RR-SECRETS | No automated secret rotation / secret-manager integration (env-var based) | M | L | H | Perilla 10 boundary checks prevent exposure; manual rotation procedure in runbook §9; Vercel/Supabase console rotation is one-step | Repo owner | Next hardening sprint | No |
| RR-WAF | No WAF/CAPTCHA in front of public endpoints | M | M | M | Perilla 9 abuse-protection (durable fixed-window limits) on signup/invite/billing/AI; Vercel platform DDoS mitigation; closed pilot = low exposure | Repo owner | Before open beta | No |
| RR-STRIPE-ENV | Stripe test-vs-live mode correctness depends on env discipline | M | L | H | Webhook signature verification + mode validation shipped (Perilla 6); runbook §6 requires explicit live-mode confirmation before pilot billing | Repo owner | Pilot start | No (runbook step) |
| RR-AI-COST | AI cost ceiling uses *estimated* prices and fails open if the accounting read fails | M | M | M | Durable fail-closed daily request ceiling backs it; price table reviewed at deploy; monitor `ai_usage_events` weekly during pilot (runbook §10) | Repo owner | During pilot | No |
| RR-EXPORT | No full workspace data export (computed-artifact exports only) | M | M | M | Closed pilot commitment: operator-run SQL export on request; product export is roadmap scope | Repo owner | Before open beta | No |
| RR-MONITOR | No external monitoring/alerting platform integration; alerts are log/audit-table based | M | M | M | Instrumentation points exist (`logSecurityEvent`, structured logger, `/api/ready`); runbook §7 defines a manual daily check cadence + the alert-worthy event list | Repo owner | During pilot | No |
| RR-LOGGER-ADOPTION | Legacy `console.*` call sites (~200) don't yet route through the redacting logger | L | M | L | Redaction utility + logger shipped and used by new/critical paths; sweep confirmed no current call site logs secrets; adopt incrementally | Repo owner | Rolling | No |
| RR-RLS-LEGACY | `onboarding_analyses` / `governance_audit_events` still on legacy `company_id` RLS (PR #148 intent) | L | L | M | Isolation still enforced via JWT claim model; rebuild #148's migration from a clean branch | Repo owner | Next hardening sprint | No |
| RR-INVITE-DELIVERY | Workspace invite links are returned to the inviter (one-time) but not emailed automatically | L | M | L | Product gap, not security (early-access flow has email delivery to mirror); inviter delivers link out-of-band during pilot | Repo owner | Roadmap | No |
| RR-ABUSE-MEMORY | In-memory abuse-store fallback doesn't share counters across serverless instances when service-role env is absent | L | L | M | Production sets service-role env → durable Supabase-backed store is used (fails closed); documented in Perilla 9 | Repo owner | — | No |
| RR-FOUNDER-PROGRAM | Founder Circle Program (Sprint 01) residuals while enabled: (R1) program governs membership, not platform entry — open signup remains the platform's pre-existing posture; (R2) operator role = founder/internal, no finer program-operator role or two-person rule; (R3) settings row edited via manual service-role SQL by design; (R4) capacity race-safety proven via the SECURITY DEFINER function + mirrored test double, but no hosted concurrency run yet (blocked on RR-MIGRATE); (R5) migration `20260828000000` not yet through any fresh-apply run. Full analysis: `docs/founder-program/11-security-review.md`. Program is default-off and fail-closed in every environment until explicitly enabled | M | L | M | Enable only via `docs/founder-program/14-launch-checklist.md`; close R4/R5 together with RR-MIGRATE's hosted run; revisit R2 before any EXPAND COHORT decision | Repo owner | Before founder cohort start | No (program off by default) |
| RR-EVIDENCE-CORRECTION | P2-15 ratified **Option A** for a changed evidence-quality judgement on an already-recorded intake attempt: `derive_operational_evidence` folds assertionType/classification/confidenceScore/missingDataState into `derivation_digest`, so the same `derivation_idempotency_key` with different quality raises `evidence_idempotency_conflict`. The recorded assertion is immutable and the correction is REFUSED, not coerced, not replayed and not minted as a second assertion. P2-15 repaired only the reporting of that refusal (stable `evidence_quality_conflict` code, human-safe message, recovery instruction, support reference id — no raw driver text). **The residual is that an observer who genuinely revises their judgement has no governed way to record the revision**; they must wait out the client attempt TTL. The preferred long-term model is Option C — governed Evidence correction/supersession — which needs a `p_supersedes` parameter on the derivation RPC, an Observation-eligibility rule excluding superseded assertions, and reconsideration of the append-only trigger. `evidence_items.supersedes_evidence_item_id` already exists (insert-time, immutable) but `derive_operational_evidence` neither accepts nor populates it, and P2-04 dropped the authenticated insert policies so the RPC is the only write path. Explicitly OUT of P2-15 scope: P2-15 authorises NO migration. | M | M | M | Design and land the governed Evidence correction/supersession contract in a migration-bearing prompt; until then the conflict is reported honestly and no duplicate assertion is created | Repo owner | Post-P2-15 / before open beta | No |
| RR-CI-DUPLICATION | P2-15 wired the existing `check:beta-release` orchestrator into `release-governance.yml` (it existed, was tested and was named a launch gate, but no workflow invoked it). Its first four gates — `build:aoc`, `typecheck`, `lint`, `test` — duplicate what `ci-governance.yml` already runs on the same events, so a PR now executes them twice. This is a deliberate P2-15 choice: correctness and independently proven release execution before CI-minute optimisation, and `ci-governance.yml` was NOT thinned to avoid weakening existing PR protection. | L | — | L | After G3, rationalise the overlap in a separate bounded change (e.g. have the beta job consume ci-governance results, or narrow the orchestrator's leading gates when run post-merge). Do not weaken existing PR protection to remove duplication | Repo owner | After G3 | No |
| RR-CRLF-LOCAL | **DISPOSITION: RESOLVED (P0-LAUNCH-05).** **The risk was real when registered and is recorded here as history, not as something that never happened.** P2-15 observed 8 source-scanning tests failing on a CRLF working copy (`adaptive-confidence`, `command-center-activation-sequence` ×3, `intervention-memory`, `playbook-engine-constitution-generator`, `pmf-004-idempotent-call-sites`, `route-guard-consistency`) because their assertions did not tolerate `\r`, and proved it independent of any code change: with the P2-15 diff stashed the same tests still failed, and converting a pristine base-commit worktree from LF to CRLF reproduced them exactly. The concern was that a BLOCKING release gate's verdict depended on the developer's line-ending configuration. **The prescribed mitigation is already on `main`:** `.gitattributes` carrying `* text=auto eol=lf` landed in P0-LAUNCH-01 (`40a7c222`), so checkouts normalise to LF on every platform. **The premise is now mechanically disproven, not merely assumed stale:** the P0-LAUNCH-05 final full battery ran the entire `tests/*.test.mjs tests/*.test.ts` glob — which contains all 8 — with **13366 tests, 13349 pass, 0 fail, 17 skip, exit 0**; the formerly failing suites were confirmed to have actually executed by matching their declared test names in the TAP stream, and a scan of the working tree found **zero** CRLF-terminated tracked source files. The 17 skips are unrelated PMF-004 concurrency tests requiring a local Postgres admin socket. | L | H | M | Closed — no further action. Re-open only if `.gitattributes` normalisation is removed or a checkout is observed carrying CRLF again | Repo owner | Closed P0-LAUNCH-05 | No |
| RR-BOOT-ENV-GUARD | **DISPOSITION: RESOLVED (P0-LAUNCH-05).** Scope of this row is now the **CLOSED-FREE-BETA environment contract**, which P0-LAUNCH-05 owns; the broader full-production contract moved to its own row, RR-PRODUCTION-ENV-GUARD, because the two are NOT the same contract and must not share a disposition. `evaluateClosedFreeBetaEnvSafety` / `assertClosedFreeBetaEnvSafety` mechanically enforce the adopted beta posture: profile selected, Supabase trio present, `NEXT_PUBLIC_APP_URL` present AND a valid http(s) URL, no secret-shaped `NEXT_PUBLIC_*` name, capability claim secret when governance is on, and **no Stripe variable required**. They are invoked by the supported entrypoint `npm run start:closed-free-beta` and PROVEN at runtime, not by source inspection: an invalid beta environment refuses to start with `next start` never reached (no Ready banner) and no surviving processes. The sibling `assertServerOnlyEnvBoundary()` - which this row recorded as having no product caller - now runs on that path and is **mechanically attributable** via a structural `guard` tag rather than prose matching, closing the gap where both guards rejected the same input indistinguishably. | L | M | M | Closed for the beta contract - no further action. The production contract is tracked separately as RR-PRODUCTION-ENV-GUARD | Repo owner | Closed P0-LAUNCH-05 | No |
| RR-PRODUCTION-ENV-GUARD | **DISPOSITION: DEFERRED_TO_P0_LAUNCH_06_WITH_EXPLICIT_REASON.** Split out of RR-BOOT-ENV-GUARD by P0-LAUNCH-05. `assertProductionEnvSafety()` still has **NO caller**, `PRODUCTION_REQUIRED_SERVER_SECRETS` is **not** narrowed, and there is no `src/instrumentation.ts` - so PMFreak performs no in-process boot-time rejection on the full-production path. **Explicit reason for deferral, and a claim that must not be misstated: the generic full-production helper is NOT wired, and P0-LAUNCH-05 does not claim it is.** Wiring it unchanged would be actively wrong for the accepted posture: it requires `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET`, which are intentionally **not** part of the closed-free-beta contract, so enforcing it at boot would refuse to start the very runtime P0-LAUNCH-05 accepted. `deployment-boundary-registry.ts` still declares Stripe production-required while `pilot-capability-set.md` (Adopted) scopes the beta to a free, no-billing surface; P0-LAUNCH-05 narrowed that contradiction to the beta path rather than resolving it globally, which is a configuration-contract decision and not a runtime defect. Not covered by any caller today on the production path: `STRIPE_*` presence, `NEXT_PUBLIC_APP_URL` validity/localhost, and platform-injected secret-shaped `NEXT_PUBLIC_*` names (the last covered statically over `src/**` by `tests/production-deployment-boundary.test.mjs`, but not against platform-injected values). | L | M | M | P0-LAUNCH-06: either narrow `PRODUCTION_REQUIRED_SERVER_SECRETS` to the adopted surface and wire the remainder at startup (`src/instrumentation.ts` `register()` - verified supported in Next 16.3.2 and NOT run during `next build`), or gate it at the deploy boundary. Resolve together with RR-BETA-PREFLIGHT-BYPASSABLE | Repo owner | P0-LAUNCH-06 | No |
| RR-AUTH-NOT-IN-READINESS | **DISPOSITION: RESOLVED (P0-LAUNCH-05), scoped to the closed-free-beta profile.** `AUTH_IS_BETA_READINESS_DEPENDENCY` is adopted and now proven AT RUNTIME with the profile actually active, not by source inspection: `AUTH_HEALTHY -> 200 ready (auth=pass)`, `AUTH_UNAVAILABLE -> 503 not_ready (auth=unreachable)`, `AUTH_RECOVERED -> 200 ready` in the SAME pid (no restart), with liveness holding 200 throughout and a non-vacuity control showing the database check still passing so the transition is attributable to auth alone. The probe calls GoTrue's anonymous `/auth/v1/health` with the ANON key under the `HEALTHCHECK_DATABASE_TIMEOUT_MS` deadline - no service-role key, no login transaction. Outside the beta profile the check is ABSENT from the reported `checks` entirely - the profile gate is at the CALL SITE, not inside the check - so the declared dependency set for non-beta consumers is unchanged and the P0-LAUNCH-04 contract is preserved, which is why its 28/28 evidence still reproduces. An earlier revision wired the check unconditionally and reported a passing `auth` entry outside the profile; that left the readiness VERDICT correct but silently widened the declared dependency set, `tests/observability-readiness.test.mjs` caught it, and the code was corrected to match the claim rather than the test relaxed. **Residual retained (by construction, not omission):** a caller with NO session stays indistinguishable during an outage, because auth-js answers `AuthSessionMissingError` locally without contacting the dependency. | L | M | M | Closed for the beta profile; P0-LAUNCH-06 rehearses it. Revisit only if the readiness contract is extended beyond the beta profile | Repo owner | Closed P0-LAUNCH-05 | No |
| RR-AUTH-ERROR-MISCLASSIFICATION | **DISPOSITION: RESOLVED (P0-LAUNCH-05).** Both call sites (`src/lib/supabase/proxy.ts`, `src/lib/auth/runtime-auth-continuity.ts`) classify on the auth-js error CLASS via the shared helper `src/lib/auth/auth-error-classification.ts`, not on "status is not 401/403". The redundant `getSession()` on the ordinary no-session path is gone; the unexpired-session fallback is entered only for `AuthRetryableFetchError` and is otherwise unchanged, as this row required. Class NAMES are used rather than `instanceof` so the result survives Next.js's separate server/edge auth-js bundles. Regression-guarded by `tests/proxy-routing.test.mjs` (asserts the helper AND `doesNotMatch` on the old comparison) and focused-gate assertions 1-5, which run against the INSTALLED auth-js classes and include two non-vacuity controls. | L | H | L | Closed - no further action | Repo owner | Closed P0-LAUNCH-05 | No |
| RR-READINESS-NOT-A-GOVERNED-GATE | A `NOT READY` instance still serves. Readiness is advisory to whatever routes traffic; it does not itself refuse governed writes. P0-LAUNCH-04 proved that governed writes nevertheless fail closed in every failure state it exercised — because the failing dependency is *on* the path (authentication, or the Frontera authority backing) — not because readiness gates them. A future misconfiguration that trips readiness while leaving the governed path intact would be served. | L | L | M | Keep the deployment layer responsible for withdrawing a NOT READY instance from rotation (runbook §7), and re-examine if a readiness-failing-but-governed-path-intact configuration is ever identified | Repo owner | Rolling | No |
| RR-BETA-PROFILE-UNEXERCISED | **DISPOSITION: RESOLVED (P0-LAUNCH-05).** The profile is no longer source-asserted only - it is exercised through the canonical supported entrypoint `npm run start:closed-free-beta` on a fresh build, with `PMFREAK_OPERATING_PROFILE=closed-free-beta` confirmed in the RUNNING process via `/proc` environ. Proven: `BETA_PREFLIGHT=ENFORCED` (invalid env refuses; `next start` never reached), `SERVER_ONLY_ENV_BOUNDARY=ENFORCED`, `VALID_BETA_ENV=STARTS`, `STRIPE_REQUIRED=NO` (started and live with both Stripe secrets blank, not merely absent), and the full auth readiness transition. A non-vacuity control shows the refusals are attributable to their injected faults rather than to beta being unstartable. | M | M | M | Closed - P0-LAUNCH-06 rehearses the proven profile rather than proving it | Repo owner | Closed P0-LAUNCH-05 | No |
| RR-BETA-PREFLIGHT-BYPASSABLE | **DISPOSITION: DEFERRED_TO_P0_LAUNCH_06_WITH_EXPLICIT_REASON.** Reason, mechanically established: the SUPPORTED local closed-beta start boundary DOES invoke the preflight and is safe - `start:closed-free-beta` is `check:beta-environment && next start`, and focused-gate assertion 22 proves an invalid environment refuses to start with `next start` never reached (no Ready banner) and no surviving processes. The remaining bypass is a bare `next start`, which belongs to an external/deployment topology (a platform that owns the start command) that **P0-LAUNCH-05 does not claim**, and `pilot-operational-runbook.md` Section 2 now explicitly marks direct/bypassing startup as **UNSUPPORTED** for the accepted local closed-beta path. This is therefore a scope boundary, not an unguarded supported path. | L | M | M | P0-LAUNCH-06: wire `assertClosedFreeBetaEnvSafety()` into `src/instrumentation.ts` `register()` so it runs in-process regardless of start command, closing this together with RR-BOOT-ENV-GUARD (B). Until then the runbook's UNSUPPORTED marking governs | Repo owner | P0-LAUNCH-06 | No |
| RR-GOVERNANCE-PERMISSION-GUARD-BROKEN | **DISPOSITION: ACCEPTED_FOR_CLOSED_BETA (P0-LAUNCH-05).** `requireGovernancePermission(workspaceId, permission)` performs its own authorization and then calls `requireWorkspaceMembership(workspaceId)`, whose `"read"` permission maps to the **project-scoped** `project.read` policy (`governance-actions.ts:19`, `governance-core.ts:28`) and is invoked with no `projectId`. The policy therefore refuses with "project scope is missing for project-scoped action" for EVERY caller, so the guard denies unconditionally. **Proven at runtime, not by reading:** a legitimate workspace OWNER received `403 ... "Denied because project scope is missing for project-scoped action."` when P0-LAUNCH-05 briefly routed offboarding through it (the 44/47 FAILED_DIAGNOSTIC_RUN). That diagnostic also invalidated two controls which appeared to pass only because the baseline was universal denial; both were withdrawn rather than counted. **Why this is nonetheless bounded for the accepted closed beta:** the defect is pre-existing; the proposed new DELETE dependency on it was REMOVED; and a mechanical inventory of every certified beta path found **zero** call sites — operator invite 0, invite acceptance 0, `/api/execution-tasks` 0, `/api/operational-flow` 0, offboarding DELETE 0 (one comment only), readiness 0, liveness 0. The ordinary request invitation path DOES reach it (`workspace-team.ts:42,75`), which is a second independent reason that path is not the certified beta admission boundary. | M | H | H | Own dedicated post-beta governance-correction increment — NOT P0-LAUNCH-06, which only rehearses the accepted architecture and re-verifies this residual stays bounded. Re-open as BLOCKING if the guard enters any certified path, if HTTP invitation becomes the supported admission boundary, if offboarding is claimed Frontera-governed, if a public/hosted mutation depends on it, or if release claims expand to "every privileged mutation traverses Frontera" | Repo owner | Post-beta governance increment | No |
| RR-BETA-OPERATOR-FRONTERA-BOUNDARY | **DISPOSITION: ACCEPTED_FOR_CLOSED_BETA (P0-LAUNCH-05).** `OPERATOR_INVITE_FRONTERA_GOVERNED=NO`, stated rather than implied. The workspace-scoped Frontera evaluator terminates in `PmfreakAccessVerificationAdapter`, whose `requireCurrentUser()` resolves the actor from an authenticated HTTP request; the beta operator command is intentionally non-request-bound, so it cannot truthfully obtain a governance decision without an architecture change that is out of scope. Verified empirically three times: the P0-LAUNCH-04 provisioning helper alone denied, an added workspace-scoped `members.manage` grant denied, and the evaluator's own composed guard confirmed the request binding. **What protects the boundary instead:** it refuses any non-local/non-isolated target BEFORE privileged access, the inviter identity must already exist, the inviter must already hold owner/admin membership in the exact target workspace, and role assignment runs the shared invitation policy so `owner` is never invitable. It is an operator-only command, not a hosted/production or self-service admission API. | L | M | M | P0-LAUNCH-06 rehearses this boundary and re-verifies the isolation guard; it must not claim the operator invite is Frontera-governed. BLOCKING if the command is permitted against hosted/non-local targets, becomes a public/server API, or becomes a production tenant-admission surface | Repo owner | P0-LAUNCH-06 (rehearse) | No |
| RR-NORMAL-INVITE-SEAT-MODEL | **DISPOSITION: ACCEPTED_FOR_CLOSED_BETA (P0-LAUNCH-05).** The ordinary request invitation path enforces subscription seat capacity, but under the observed default configuration `company_subscriptions` is empty, so every company resolves to the `free` plan, and **both `free` and `pro` declare `seat_limit: 1`** (`feature-gates.ts`). The workspace owner already occupies that seat, so `usedSeats < seatLimit` is false and the ordinary path can never admit an additional participant. It also reaches `RR-GOVERNANCE-PERMISSION-GUARD-BROKEN` first. This makes ordinary self-service invitation unsuitable as the certified closed-beta admission path, but does not block the accepted operator-controlled model: `BETA_OPERATOR_SEAT_POLICY=OPERATOR_CONTROLLED_NOT_SUBSCRIPTION_GATED`, `OPERATOR_INVITE_SUBSCRIPTION_SEAT_GATED=NO`. The beta has no billing surface and admission count is controlled by the Founder/operator cohort. Commercial parity between the two paths is explicitly NOT claimed. | M | M | M | Resolve product/commercial seat semantics before any public/self-service/paid onboarding. P0-LAUNCH-06 rehearses the operator-controlled model and does not redesign entitlement semantics. **BLOCKING for public/self-service/paid onboarding**; re-open if the operator-controlled beta restriction is removed | Repo owner | Before public/paid onboarding | No |
| RR-OFFBOARD-AUDIT-NONATOMIC | **DISPOSITION: ACCEPTED_FOR_CLOSED_BETA (P0-LAUNCH-05).** Membership deletion and the `member_removed` audit insert are two separate persistent writes with no enclosing transaction, so an audit failure can occur AFTER effective authority has already been removed. What P0-LAUNCH-05 closed is silence, not the window: the insert result is now inspected and a failure raises `WorkspaceMembershipAuditError`, which the route surfaces as `500 offboarding_audit_write_failed` instead of a clean success. **Both directions proven at runtime:** a successful removal persists `member_removed` with the correct workspace, actor, target and `previousRole`; and an injected audit failure returns 500 with the membership already deleted and no audit row — the partial state itself, observed. Atomicity is NOT claimed. | L | M | M | **Operational response on this failure class: inspect effective membership FIRST, treat it as an incident requiring reconciliation, and do NOT blindly retry the deletion.** A transactional fix needs schema/RPC work deliberately excluded from P0-LAUNCH-05 | Repo owner | Post-beta | No |
| RR-INVITE-AUDIT-NONATOMIC | **DISPOSITION: ACCEPTED_FOR_CLOSED_BETA (P0-LAUNCH-06).** `createWorkspaceInvitationRecord` (`src/lib/workspace-team.ts`) performs the `workspace_invitations` insert and the `invitation_sent` audit insert as two separate persistent writes with no enclosing transaction, and — unlike the invitation insert, whose error is checked and thrown — the audit insert's returned error is **not inspected at all**. An invitation can therefore be created and be fully usable while its `invitation_sent` audit row is silently missing. Same class as [`RR-OFFBOARD-AUDIT-NONATOMIC`](#). Found during the P0-LAUNCH-06 PR review; product code deliberately NOT changed in that increment. Detection: the P0-LAUNCH-06 rehearsal's C2 control now binds the audit assertion to the specific invitation under test (payload email, role, inviter, and expiry instant, with the query error asserted), so a missing audit row for this run's invitation fails the gate instead of being masked by an older seeded event. Blast radius is an audit-completeness gap, not an authority gap: no invitation is granted or elevated by the missing row. | M | L | M | Accepted for the closed beta: operator-scale invitation volume with a named operator, and the rehearsal control now detects a missing audit row for the invitation under test. Fix before open beta by making the invitation row and its audit event atomic (single transaction or RPC), or at minimum inspecting the audit insert error | Repo owner | Before open beta | No |
| RR-BETA-PLATFORM-SIGNUP-OPEN | **DISPOSITION: ACCEPTED_FOR_CLOSED_BETA (P0-LAUNCH-05).** Platform identity creation remains available under the pre-existing product posture, but identity creation does not confer admission to the closed beta, tenant membership, tenant role, or governed authority. Beta participation remains invitation/operator controlled at the tenant-authority boundary. The accepted model is `CLOSED_BETA_AUTHORITY_MODEL=INVITATION_CONTROLLED_TENANT_AUTHORITY`; `PLATFORM_SIGNUP_IS_DISABLED` is **NOT** claimed anywhere. Mechanically proven with a real non-invited identity: it exists and authenticates, holds no tenant membership and no tenant role, is denied tenant read (403) and governed tenant operation (403 authorization); admission through the supported operator boundary then establishes membership and the intended role, and the SAME session's tenant read moves 403 -> 200 with no re-login; offboarding returns it to 403 while the `auth.users` row survives. **Release implication: NONE for the accepted closed-beta authority model.** | L | M | L | P0-LAUNCH-06 should REHEARSE and re-confirm this boundary, not redesign signup - unless new evidence shows that signup can confer beta authority, in which case this becomes blocking | Repo owner | P0-LAUNCH-06 (rehearse) | No |

## Closed

### RR-POSTCSS — bundled PostCSS advisory — **Closed** (2026-08-24, P2-15)

* **Was**: a PostCSS advisory bundled by `next@16.2.10`, temporarily accepted as a low-risk build-time residual while no stable patched Next release was available.
* **Closure**: P2-15 upgraded `next` and `eslint-config-next` to `16.3.2`; the `next`, `postcss`, `sharp`, and `nanoid` security findings disappeared.
* **Verification**: final LF clean-room dependency security reported 2 findings, 2 accepted, 0 unexpected; the remaining findings are the separately reviewed `exceljs` / transitive `uuid` moderate advisory.
* **Control cleanup**: obsolete `postcss` and `next` accepted-risk entries were removed so future advisories for either package must be evaluated afresh.

### RR-XLSX — vulnerable `xlsx@0.18.5` spreadsheet parser — **Closed** (2026-07-11, Perilla 12)

* **Was**: `xlsx@0.18.5` (last npm-published SheetJS build) with high
  prototype-pollution (GHSA-4r6h-8v6p-xvw6) and ReDoS (GHSA-5pgg-2g8v-p4x9)
  advisories, reachable from the untrusted evidence-upload parse path; no
  npm fix; vendor CDN (cdn.sheetjs.com) blocked by build-env egress (403,
  re-verified 2026-07-11), so the Perilla 11 condition ("install the CDN
  build") was not executable.
* **Closure**: dependency **removed** and replaced with `exceljs@4.4.0`
  (npm registry, lockfile-pinned) behind a new internal abstraction
  (`src/lib/spreadsheets/`), per
  [`xlsx-replacement-decision.md`](./xlsx-replacement-decision.md).
  Closure commit: the Perilla 12 PR
  (branch `claude/xlsx-dependency-security-35m85v`).
* **Audit evidence**: `npm ls xlsx` → `(empty)`; `npm audit` → 0 critical,
  0 high (the xlsx findings are gone; the one new moderate — transitive
  `uuid` via exceljs — is unreachable, see
  [`dependency-security-review.md`](./dependency-security-review.md)).
* **Test evidence**: `tests/spreadsheet-reader-contract.test.ts`,
  `tests/spreadsheet-security-boundary.test.ts`,
  `tests/spreadsheet-export-workbook.test.ts`,
  `tests/spreadsheet-dependency-boundary.test.mjs`,
  `tests/prototype-pollution-guard.test.mjs` — all green in the full suite.
* **Lasting controls**: `xlsx` is permanently forbidden
  (`scripts/check-dependency-security.mjs` FORBIDDEN_PACKAGES +
  `tests/spreadsheet-dependency-boundary.test.mjs`); the untrusted-parse
  boundary (size/sheet/row/column/cell caps, macro/external-link/OLE/zip-bomb
  rejection, pollution canary, parse deadline) is documented in
  [`../security/spreadsheet-processing-boundary.md`](../security/spreadsheet-processing-boundary.md).

## Resolved in Perilla 11 (removed from the register)

* Plaintext workspace invite tokens → hashed storage, plaintext column
  dropped, legacy invites revoked, replay-proof (Perilla 11 §C).
* Unbounded AI execution/cost → timeout/retry/concurrency/depth/daily
  request+cost ceilings, circuit breaker, usage accounting (§E).
* No readiness probe / no structured redacting logger → `/api/ready`,
  `src/lib/observability/logger.ts` (§F).
* `next` 16.2.4 highs (middleware bypass, DoS, SSRF), `ws` high, and five
  fixable moderates/lows → upgraded/fixed (§B).
* Non-timestamped migration file (`create_dashboard_task_lifecycle.sql`) →
  renamed + idempotent policies (§G).
* `check:launch-readiness` ENOBUFS false-failure → buffer fix (§A).
